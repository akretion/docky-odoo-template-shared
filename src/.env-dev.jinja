{# `cp .env-dev .env` or `copier copy https://github.com/akretion/docky-odoo-template-personal .` #}
ENV=dev
COMPOSE_FILE=docker-compose.yml:dev.docker-compose.yml
COMPOSE_PROJECT_NAME={{project_name}}
PROJECT_NAME={{project_name}}
DOMAIN={{project_name}}.localhost
